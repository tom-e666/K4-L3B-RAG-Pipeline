# Báo cáo đóng góp cá nhân — Nguyễn Đức Long

## Thông tin

- Họ và tên: Nguyễn Đức Long
- Mã học viên: `2A202602917`
- Nhóm: T021
- Repository/branch: `tom-e666/K4-L3B-RAG-Pipeline`, `dlong` → `main`
- Nguồn xác minh: commit tác giả `Long <duclongt23@gmail.com>` và các merge PR #1–#5 trong Git.

## Phần việc đã thực hiện

| Module/deliverable | Việc thể hiện trong commit | File/commit | Trạng thái |
| --- | --- | --- | --- |
| Weighted RRF, indexing/retrieval | Bổ sung trọng số fusion và cập nhật luồng retrieval | [`f5876c8`](https://github.com/tom-e666/K4-L3B-RAG-Pipeline/commit/f5876c8), `src/task4_chunking_indexing.py`, `src/task7_reranking.py`, `src/task9_retrieval_pipeline.py` | Đã merge |
| LLM listwise rerank | Thêm reranker hậu truy xuất và tích hợp vào pipeline | [`2989b37`](https://github.com/tom-e666/K4-L3B-RAG-Pipeline/commit/2989b37), `src/task7_reranking.py`, `src/task9_retrieval_pipeline.py` | Đã merge qua PR #2 (`d9c18b6`) |
| Grounding và citation gate | Bổ sung prompt grounded và kiểm tra citation cho generation | [`db1d6a4`](https://github.com/tom-e666/K4-L3B-RAG-Pipeline/commit/db1d6a4), `src/task10_generation.py` | Đã merge |
| HyDE | Bổ sung hypothetical document trước dense search | [`9f3bcef`](https://github.com/tom-e666/K4-L3B-RAG-Pipeline/commit/9f3bcef), `src/task5_semantic_search.py` | Đã merge |
| MMR context packing | Chọn context đa dạng trước generation | [`00caf70`](https://github.com/tom-e666/K4-L3B-RAG-Pipeline/commit/00caf70), `src/task10_generation.py` | Đã merge |

## Quyết định kỹ thuật được thể hiện trong mã

1. **Weighted RRF và listwise rerank:** kết hợp dense/BM25 theo trọng số rồi dùng LLM sắp xếp lại ứng viên. Trade-off quan sát trong eval DeepSeek: listwise tăng chất lượng trên golden set nhưng thêm độ trễ; weighted RRF riêng lẻ chưa cải thiện trung bình so với A dense-only.
2. **HyDE và MMR:** mở rộng truy xuất semantic trước tìm kiếm, sau đó chọn context giảm lặp nguồn. Ablation mới nhất cho thấy HyDE chỉ tăng 0.008 điểm trung bình so với B không HyDE, thêm 1.739 giây p50 và context recall giảm 0.031; cần tiếp tục hiệu chỉnh.

## Kiểm thử và kết quả liên quan

Bộ test hiện tại đạt 25/25. Lượt DeepSeek mới nhất trên 16 case cho A dense-only 0.878 và B full 0.893 trung bình bốn metric; rerank fallback 0/32, HyDE lỗi 0/16, MMR lỗi 0/32. Đây là kết quả đánh giá của nhóm trên các kỹ thuật trong commit của Long, **không chứng minh Long cá nhân đã chạy lượt eval**. Xem [evaluation/RESULT.md](../group_project/evaluation/RESULT.md) và [báo cáo nhóm](GROUP_REPORT.md).

## Hạn chế và xác nhận

Context recall B full thấp hơn A 0.063; `multi-01` còn thiếu evidence ở chunks được chọn. PageIndex chưa được triển khai. Báo cáo dựa trên Git và kết quả nhóm; Nguyễn Đức Long cần kiểm tra và xác nhận phần tự thuật trước khi nộp.
